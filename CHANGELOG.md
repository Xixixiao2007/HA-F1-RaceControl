# 更新日志

## v2.0.5

versionCode 5 → 6。

### 修复：`SESSION` 译成「会话」是误译

`SESSION` 指的是**这一个比赛环节**（一练 / 排位 / 正赛），简称就是**比赛**。
「会话」是计算机味的直译，比赛时看着莫名其妙。

| 原文 | 改前 | 改后 |
| --- | --- | --- |
| `SESSION WILL RESUME AT 17:47` | 会话将于 17:47 恢复 | **比赛将于 17:47 重启** |
| `SESSION WILL BE TEMPORARILY STOPPED` | 会话暂时中止 | **比赛暂时中止** |

同一个词在标题栏里也错了 —— 红旗时右侧显示的是英文 `SESSION SUSPENDED`。
中文界面上最要紧的那个状态，旁边挂着一串英文。现在全中文：

| 状态 | 改前 | 改后 |
| --- | --- | --- |
| 红旗 | `SESSION SUSPENDED` | **比赛暂停** |
| 安全车 | `SAFETY CAR` | **全场** |
| 虚拟安全车 | `VIRTUAL SAFETY CAR` | **全场** |

「全场」和双黄旗的「区段 12,13」是一组对照：整条赛道 vs 某几个区段。

### 改变：长消息不再截断

列表里的**原文**原来限 3 行 + 省略号 —— 那两条 200 字的仲裁消息会被切掉一半。
现在原文和译文都**不限行数**，一条消息占几行就占几行（上一版只放开了译文）。

---

## v2.0.4

versionCode 4 → 5。

### 修复：全周末最长的两条消息一条译文都没有

```
FIA STEWARDS: Q1 INCIDENT INVOLVING CARS 81 (PIA), 63 (RUS), 3 (VER), 5 (BOR),
27 (HUL), 10 (GAS), 43 (COL), 22 (TSU) AND 77 (BOT) NO FURTHER ACTION -
FAILING TO FOLLOW RACE DIRECTORS INSTRUCTIONS - MAXIMUM DELTA TIME
```

`NO FURTHER ACTION` 这个写法不认识 —— 它是「不予追究」，恰恰是最该看懂的一类。
现在认得，并且**车号一个不省**：

> 仲裁：Q1 皮亚(81)、拉塞尔(63)、维斯塔潘(3)、霍肯伯格(27)、加斯利(10)、
> 科拉平托(43)、角田(22)、博塔斯(77) —— 不予追究：未遵守赛会指令（超出最大圈速差）

列表里的译文**同时放开了行数限制**（原来限两行 + 省略号，正好会把后半截车号切掉）。

### 修复：几处信息被丢掉

| 丢的是什么 | 改前 | 改后 |
| --- | --- | --- |
| 复合原因的后半截 | `未遵守赛会指令` | `未遵守赛会指令（超出最大圈速差）` |
| 排位赛阶段 | `赛事事故` | `Q1 事故` |
| 括号套括号 | `不予追究（未遵守赛会指令（逃生通道））` | `不予追究：未遵守赛会指令（逃生通道）` |

### 新增：内容型消息的中文

`YELLOW IN PIT LANE` → 维修区黄旗、`PIT LANE CLEAR` → 维修区解除、
`SESSION WILL RESUME AT 17:47` → 会话将于 17:47 恢复、
`MARSHALS ON TRACK AT TURN 20` → 20 号弯有马修、`MEDICAL CAR DEPLOYED` → 医疗车出动、
`FIRST CAR TO TAKE THE FLAG - CAR 5 (BOR)` → 首个冲线：博托莱托(5)、
`RISK OF RAIN FOR F1 FREE PRACTICE 2 IS 0%` → F1 二练降雨概率 0% …… 共 48 条。

判据是「**类型徽标表达不出来的信息才翻**」：`DOUBLE YELLOW IN TRACK SECTOR 15`
的徽标就是双黄旗，不重复翻；`YELLOW IN PIT LANE` 的徽标只说黄旗，所以要翻。

### 术语定名

- **`TRACK SECTOR` → 区段**。它是**马修站分段**（按马修站/灯牌分段），
  不是一圈三个的计时分段，也不是 mini sector。用「区段」既不跟计时分段抢「赛段」，
  也不是几何意义上的「扇区」。标题栏、全屏横幅、设置页、文档一起改。
- **马歇尔 → 马修**（marshal 在中文 F1 圈的定名）。
- 车手译名缩短：皮亚斯特里 → **皮亚**、林德布拉德 → **林布拉德**、
  博尔托莱托 → **博托莱托**。

### 测试

新增 19 条回归测试钉住上面这些（多车名单、复合原因、Q 阶段、马修、区段）。
`tools/run_tests.py` 153 → 173 项；全套 7 项全过。

---

## v2.0.3

versionCode 3 → 4。

### 标题栏改成旗语，去掉重复的状态条

原来屏幕上占两行：标题栏显示 `F1 Race Control · sensor.f1_race_control_2`，
下面单独一条 26sp 的大状态条显示旗语。

实体 ID 只有配置的时候有用，比赛时就是占地方。现在**标题栏本身就是旗语条**：
整条按当前级别上色，左边大号旗语（22sp），中间区段明细，右边设置键。
下面那条状态条删掉了 —— 屏幕上只剩「标题栏 + 状态行 + 列表」。

顺带修掉一个会看不清的细节：设置键的颜色跟着底色走。
双黄旗、黄旗、VSC 是浅色底，白字会糊成一片（`TrackState.textColor()` 管这件事）。

未配置时标题栏不显示旗语（没意义），改回中性深色并提示去设置页填。

---

## v2.0.2

versionCode 2 → 3。

### 修复：拿「消息里含某个短语」当类型判据

原规则写的是「消息含 `TRACK LIMITS` 就算删圈速通报」。这条把**黑白旗**也吞了：

```
BLACK AND WHITE FLAG FOR CAR 44 (HAM) - TRACK LIMITS
```

它确实提到超赛道限制，但它**不是**删圈速通报，是给车手的警告。

而且这个错误是**结构性**的 —— 赛会消息和数据通报里都可能出现这个理由。
今天赛会调查类恰好没被吞（措辞里没有这四个字），**但那是运气**。

现在改成认**消息形状**：删圈速通报一律以 `CAR ` 开头且含 `DELETED`，
无论正文写什么理由都不会误吞；另外显式写死一批**永不隐藏**的
（黑白旗、判罚、`FIA STEWARDS`、`INCIDENT`、`PIT LANE`、`SESSION`）。

- 修掉 4 条被误伤的黑白旗
- 多抓 5 条旧规则漏掉的（因双黄旗违规删的圈速，它们不含 `TRACK LIMITS`）
- 赛会调查开始 / 不予追究 / 维修区状态 / 会话控制，从此**结构性**不可被隐藏

### 修复：格子旗是蓝的、蓝旗是紫的

- **格子旗**原本刷成蓝色，跟蓝旗撞车。新增 `CheckerDrawable`，画成**灰白棋盘格**
  —— 字面意义上的格子旗。左侧色条也画成小格。
- **蓝旗**原本是紫色（`0xFF7B1FA2`），改回蓝色（`0xFF1976D2`）。

### 修正：文档里算错的噪音占比

早先写的「正赛 64.6%」是 42.9%（蓝旗）+ 21.7%（删圈速）**手算**加起来的，
**漏掉了 CLEAR 和「不予追究」**。用代码把 `isNoise()` 逐条跑 697 条真实数据
重算才纠正：默认隐藏 **54.8%**（全周末）/ **68.5%**（正赛）。

> **教训：百分比不要手算，让代码数。**

按新规则，`TRACK LIMITS` 造成的后果在真实数据里是**删圈速 + 黑白旗**，
没有一条判罚是由它引起的 —— 翻译规则据此校正。

### 工程

- 发布工具：`git push` 不通时自动降级走 GitHub REST（逐个提交重放，每步核对 SHA）；
  新增 `tools/test_release_meta.py` 对象重建自测，已并入全套测试第 ④ 项。
- 修掉 Windows GBK 控制台下若干入口脚本的打印崩溃（子进程输出含非 UTF-8 字节所致）。

---

## v2.0.1

versionCode 1 → 2。

### 新增

- **判罚/仲裁的中文简述**（`Translator`）—— 原文又长又全是术语，比赛时来不及读。
  列表和全屏横幅都会在下面补一行，例如
  「★ 判罚：塞恩斯(55) 罚时 5 秒 —— 维修区超速」。
  规则全部从真实数据的措辞反推；**翻不出来就不显示**，不瞎猜。
- **设置页加了「试听」** —— 轻提醒 / 超强提醒 / 全屏横幅各一个按钮，
  不用连 HA 就能当场验证声音和震动到底出不出来。
- **手感测试台**（`tools/try_feel.py`）—— 起一个假 HA，**按键就往手机推一段剧本**，
  不用等比赛、不用重启服务器。
- **发布守卫**（`tools/release.py`）—— 见下面「工程」。

### 修复

- **轻提醒完全没感觉**（真机上既没声音也没震动）。两个独立原因：
  1. v1.4 用的是 `RingtoneManager.getRingtone(...).play()`，在用户那台 Android 6 上
     验证过能用；v2.0 换成了 `MediaPlayer` + `setDataSource(ctx, settingsUri)`，
     而「设置铃声」是 `content://settings/system/...` 这种设置项 URI，
     `prepare()` 在部分设备上会静默失败。**已退回 Ringtone。**
  2. `attention()` 先起震动、随后调用 `stopAlarm()`，而 `stopAlarm()` 里有
     `vibrator.cancel()` —— 刚起的震动被自己立刻取消了。
     已把「停声音」和「停震动」拆成两个方法。
- 翻译里两处：`已执行5 秒罚时` 少个空格；`(PIT)`（进站标记，真实数据里出现 41 次）
  被车号正则误当成车手，翻译里多出个不存在的人。

### 改变

- 「强提醒」在设置页和文档里统一改名为「**超强提醒**」。
- **测试剧本里的消息全部换成真实数据里的完整句子**（逐字取自 TSV）。
  原来写的是 `TRACK LIMITS AT TURN 4` 这种编出来的短句 —— 真实数据里没有这个形状。
  用假短句测，测的就不是分类/过滤/翻译的真实输入。新增 `penalty` 剧本。
- 新增的「**超强提醒**」文案与试听按钮见上。

### 工程

- **修掉了自己的一个坏习惯：换内容不换版本号。**
  v2.0.0 期间连着重建了 4 次、每次覆盖同一个 Release 资产，结果 4 个不同的 APK
  全叫 `2.0.0`（versionCode 都是 1）—— 用户分不出自己装的是哪一个，
  先下载的人手里还是旧文件。
  现在 `tools/release.py` 有两道闸：
  1. **本地**：源码变了但 versionCode 没变 → 拒绝构建发布
  2. **远端**：该版本号已发布过、但资产内容不一样 → 拒绝（把远端资产下回来逐字节比）
  一道是纪律，一道是兜底；后者即使本地状态文件被删也照样拦得住。

---

## v2.0.0

**从 `Android_HA_Viewer` v1.4 分叉，特化为 F1 赛事控制专用 App。**

上一代是通用的「任意实体变化流水」；这一代只服务 F1 赛事控制，
并且把「比赛日实际会发生什么」用真实数据摸清了才动手。

包名改为 `com.haf1.racecontrol`（原来 `com.haf1.entitylist`），
所以新老两个 App **可以同时装在一台手机上**。

### 设计依据：一个完整比赛周末的实测

2026-09-11 ~ 09-13，697 条真实消息、5 个会话。下面每个数字都改变了设计：

| 实测事实 | 对设计的影响 |
| --- | --- |
| 蓝旗一项就占正赛消息的 42.9%，加上解除和删圈速通报构成主要噪音 | 默认噪音过滤 |
| 142 条双黄消息只对应 24 个真实事件 | 必须做事件聚类 |
| 每条双黄都报警 = 146 次/周末 | 按「事故」聚合 + 滤掉测试型 → **23 次**（实测） |
| 双黄存活时长最短 1 秒、中位 42 秒 | 「存活 ≤15 秒」= 系统测试 |
| 峰值 7 条/秒 | 闪动要合并，不能并发跑动画 |

> 「噪音占多少」取决于过滤规则怎么定义，v2.0.2 换了判据之后重新量过
> （54.8% / 68.5%），见那一节。这里只放与规则无关的数据事实。
| 红旗一个周末只 3 次 | 红旗不受冷却期限制 |

### 新增

- **旗语分类器**（`Classifier`）—— 只认结构化字段，不用文本猜
- **提醒闸门**（`AlertGate`）—— 事件聚类、测试型判定、分级冷却
- **赛道状态机**（`TrackState`）—— 展示优先级 RED > SC > VSC > 双黄 > 黄
- **超强提醒全屏横幅**（`AlertActivity`）—— 重复警报音 + 震动 + 点亮屏幕，锁屏可见
- **通知栏留存**（`Notifier.pushNotification`）—— 全屏横幅转瞬即逝，
  你要是正好没看手机就什么都不知道了；通知栏那条会留着，锁屏上也可见
- **消息仓库**（`MessageStore`）—— 去重、容量控制、落盘
- 顶部赛道状态条、默认噪音过滤、按车号筛选、丢号检测、本地留存

### 修复了两个「照抄上游文档就会写错」的坑

上游集成作者自己的自动化蓝图里，这两处取值是错的 —— 照抄会写出**永远不触发**
或**永远漏判**的逻辑：

- **安全车 / VSC 没有 `flag`**。蓝图把 `VSC` `SC` 列为 flag 取值，
  真实数据里是 `flag=""` + `category="SafetyCar"`。必须按 `category` 判。
- **旗语是 `BLACK AND WHITE` 一个值**，不是 `BLACK` / `WHITE` 两个。

两处都有回归测试钉住。

### 重要的行为变化

- **双黄旗不再立刻强提醒**：先轻提醒，存活超过 15 秒才升级为全屏强提醒。
  这样既不会为系统测试炸响，也不会漏掉真事故（闪动是即时的）。
  警报单位是「一次事故」而不是「一个区段」，事故之间按 45 秒静默间隔划界。
  实测一个周末 **23 次**警报（3 红旗 + 1 VSC + 19 双黄事故）。
  可在设置里改成「只闪动」「只轻提醒」或「仅大面积（≥3 区段）升级」。
- **强提醒默认突破静音模式**（走闹钟通道）。可在设置里关。
- **历史回溯默认 12 小时**（原来 24 小时）：因为分类需要属性，
  而 `minimal_response` 会把属性抹掉，只能取完整响应，代价是响应体变大。
- **去重键改用 `event_id`**：实测存在连续两条**文本完全相同**的消息，
  靠文本+时间戳判定不可靠。
- **强提醒会点亮屏幕并盖住其它 App**，比赛时你可能正在看别的页面。

### 工程

- `build_apk.py` 不再覆盖清单里的版本号（上一代固定传 `1` / `1.0`，
  而清单里写的是 `5` / `1.4`，产物到底是哪个全看工具实现细节 —— 不该留这种不确定性）
- 桌面单测从 65 项扩到 **117 项**
- 新增 `tools/mock_ha.py`：本地假 HA（REST + WebSocket，116 项自测通过），
  可回放 697 条真实比赛数据，也能跑红旗/安全车/VSC/测试型双黄/突发等剧本
- 新增 `tools/run_e2e.py` + `IntegrationTest`：**集成测试**。
  把真实的产品代码（`HaClient` / `HaWebSocket` / `AlertGate` / `TrackState`）
  编出来，隔着真实 socket 连 mock 服务器跑。这台机器上装不了 APK，
  而"两个类各自单测通过"不等于"它们按真实协议能连上"。
  它验证了：697 条真实数据的属性解析、分类两个坑、以及警报次数（23 次）
- 桌面测试用的 `org.json` 桩改成**真正的 JSON 解析器**。
  原来的空桩永远返回兜底值，会让集成测试"全绿但什么都没验证" —— 那比没有测试更糟

---

> 以下 v1.x 属于分叉前的 `Android_HA_Viewer`，历史保留以便追溯。

## v1.4

- 修复：**同一条消息会显示两遍**（真正的根因，前两个版本都改错了方向）
  - HA 的历史接口会在响应开头塞一条「**期初状态**」——该区间开始时仍然生效的那个值——
    并把它的 `last_changed` 设成**我们请求的起始时刻**，而不是这个状态真正变更的时间。
    实测：请求 `start=12:20:53`，返回的首条 `last_changed` 就是 `12:20:53`，
    而该状态真实的变更时间是 `12:19:46`。
  - 而 App 的增量对账用的是 `start = 最新一条变化的秒`。于是这条期初状态
    （内容 = **上一条**消息）会顶着「**最新那条消息的时间**」再被插入一次。
  - 这正是用户观察到的现象：**同一段文本、两个不同时间戳，且其中一个等于新消息的时间**。
  - 修法：丢弃历史响应里的首个条目。判据见 `HaClient.isSyntheticStartState`
    （首条 + 带完整字段 + 时间戳落在请求的起始秒上）。
  - 用真实 HA 做过对照验证：修之前 45 条里有 1 组重复，修之后 43 条 0 组重复。
- 去重键改回**原始的 `last_changed` 字符串**。对真实 HA 实测确认：WebSocket 实时推送与
  REST 历史接口对同一次变化给出的 `last_changed` 和 `state` **逐字节完全相同**，
  v1.3 里"两条通道精度不同"的假设不成立。因此不需要秒级粒度，也就不会把同一秒内的
  两次不同变化误合并。
- 单元测试增至 65 项

## v1.3

- 修复：同一条变化重复出现（**这是一次失败的排查，方向错了**）
  - 当时的假设：WebSocket 的时间戳来自内存状态机（带微秒），而 REST 读的数据库会抹掉微秒，
    两者字符串不同导致去重失效。于是把去重键从原始字符串改成「秒级时间戳 + 值」。
  - 后来用真实 HA 实测证明这个假设不成立：数据库**保留微秒**，两条通道逐字节一致。
    改动本身无害（去重更宽松），但没有解决任何问题。真正的原因见 v1.4。
- 单元测试从 58 项增至 64 项

## v1.2

- **新增 WebSocket 实时推送**：改用 `subscribe_trigger`，由服务端按 `entity_id` 过滤，
  延迟从秒级降到百毫秒级（局域网约 50~150 ms）
- 实时断线自动重连，指数退避 1→2→4→…→30 秒
- 重连期间自动降级为 REST 轮询，列表不中断
- 实时在线时每 60 秒做一次 REST 对账，兜住重连空档
- 提示音增加 2 秒最小间隔节流（实时推送下事件逐条到达，不节流会连环炸响）
- 设置页新增「实时推送」开关
- 标题栏显示当前通道：`⚡ 实时` / `⏳ 连接中` / `⏱ 轮询`

## v1.1

- 新增提示音（系统通知铃声，跟随筛选结果，首次历史回溯不响）
- 刷新间隔改为可配置，默认由 3 秒降到 2 秒
- 修复：窗口内无变化时会被一直判定为「首次加载」，导致重复全量拉取；
  叠加提示音后还会表现为「永远不响」

## v1.0

- 首个可用版本
- 基于 REST 历史接口的增量轮询
- 关键词黑白名单筛选（开关切换模式）
- 新记录自动置顶
